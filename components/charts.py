""" creates charts """
from matplotlib import pyplot as plt

def bar_chart(input_dict, x_label, y_label):
    chart = plt.figure()
    return chart

def pie_chart(input_dict, x_label, y_label):
    chart = plt.figure()
    return chart